{% extends 'layouts/library.layout.njk' %}

{% set pageTitle = 'Pattern Library - Banners' %}

{% block content %}

  {{ components.captionHeading(text='Default banner', tag='h2', size='m') }}

  {{ components.dvsaBanner(mainText='Heading content', secondaryText='You have sucessfully submitted your application') | prism }}

  {{ components.captionHeading(text='Info banner', tag='h2', size='m') }}

  {{ components.dvsaBanner(mainText='This is a draft copy of the MOT manual', secondaryText='The final version is due to be released in XYV 2018', type="info") | prism }}

  {{ components.captionHeading(text='Error banner', tag='h2', size='m') }}

  {{ components.dvsaBanner(mainText='This is the end', secondaryText='Of our elaborate plans, the end', type="error") | prism }}

  {{ components.captionHeading(text='Confirmation banner', tag='h2', size='m') }}

  {{ components.dvsaBanner(mainText='I say yeh, yeh', secondaryText='That\'s what I say I say yeh, yeh', type="confirmation") | prism }}

{% endblock %}
