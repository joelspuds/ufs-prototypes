{% extends 'layouts/default.layout.njk' %}

{% set pageTitle = 'Design System' %}

{% block content %}

  {{ components.pageHeading('Front-end', 'Build v' + repoPackageJSON.version) }}

  {% call components.gridRow() %}

    {% call components.columnOneThird() %}

      {{ components.captionHeading(text='Styleguide', tag='h2', size='m', url='/library') }}
      {{ components.paragraph(text='GDS and custom components library to be used by all applications.') }}

    {%- endcall %}

    {% call components.columnOneThird() %}

      {{ components.captionHeading(text='Prototypes', tag='h2', size='m', url='/prototypes') }}
      {{ components.paragraph(text='All prototypes and user journeys.') }}

    {%- endcall %}

  {%- endcall %}

  {% call components.gridRow() %}

    {% call components.columnTwoThirds() %}

      {{ components.captionHeading(text='About the Design System', tag='h2', size='m') }}
      {{ components.paragraph(text='The Design System generates versioned images, CSS and JavaScript and provides a platform to document pattern usage, design principles, user insight and allow for rapid prototyping.') }}

    {% endcall %}

  {% endcall %}

{% endblock %}
