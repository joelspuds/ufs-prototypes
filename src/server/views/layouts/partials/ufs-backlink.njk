{% if backLinkContents %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <a href="{{ backLinkContents[0].url }}" class="govuk-back-link">{{ backLinkContents[0].text }}</a>
    </div>
</div>
{% endif %}
